package Database;

import org.example.projectworkspace.Authentication.User;
import org.example.projectworkspace.Authentication.userDAO;
import org.example.projectworkspace.Flights.Flight;
import org.example.projectworkspace.Flights.flightDAO;

import java.sql.Date;
import java.sql.Timestamp;

// Test class to test database operations using methods

public class TestStatements {

    public static void main(String[] args) {


















        /*

        // Create an instance of userDAO
        userDAO userData = new userDAO();

        // System.out.println(userData.getUserById(12)); (Returns a user by ID)

            //userData.deleteUser(12);

            // User userTest = new User("John", "Green", "123", "12345", "123456789", "111 Central Av", "NY", "johngreen@gmail.com", "Favorite TV show?", "Friends", "admin", "johnGreen", 12);

            //  userData.createUser(userTest);



        // Create an instance of flightDAO
        flightDAO flightData = new flightDAO();

        // Timestamp objects
        Timestamp takeoff = Timestamp.valueOf("2024-02-01 09:30:00");
        Timestamp landing = Timestamp.valueOf("2024-02-01 11:00:00");

        // Create a flight object

        Flight flight1 = new Flight(1, "Atlanta", "Miami", 189, 155, takeoff, landing, Date.valueOf("2024-02-01"), "Delayed");

        // Pass new flight object through the createFlight method

            // flightData.createFlight(flight1); (Commented for reference, code has already ran)

            // flightData.deleteFlight(1); (Commented for reference) , code successfully deleted a flight

        // Update a flight

        Timestamp newTakeoff = Timestamp.valueOf("2024-03-01 10:00:00");
        Timestamp newLanding = Timestamp.valueOf("2024-03-01 12:00:00");
        Date newDate = Date.valueOf("2024-03-01");

            // flightData.updateFlight(2, "New York", "Los Angeles", 180, 150, newTakeoff, newLanding, newDate, "On Time");


        */

    }
}